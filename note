#!/bin/sh
cd ~/Documents/notas

case "$1" in
	"-l") for f in *; do
			echo $f
		done
		exit 0;;

	"-o")$EDITOR "$2"; exit 0;;
	"-r")rm "$2"; exit 0;;
esac

nombre="$1"\ 
fecha=$(date +"%y-%m-%d")
test "$1" = "" && titulo=$fecha || titulo="$nombre"$fecha

$EDITOR "$titulo"
